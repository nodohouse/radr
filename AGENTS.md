# RADR — agent instructions

RADR is **Verified Decision Intelligence for hospitality** — the decision layer above POS / PMS / labor / RMS / channels.

Not: restaurant management, hotel PMS, RMS, scheduling, BI, finance dashboard, reservation platform, or a generic AI copilot.

## What RADR answers

WHAT CHANGED? · WHAT MATTERS? · WHAT IS LIKELY? · COST OF DOING NOTHING? · OPTIONS? · BEST OPTION? · HOW SURE? · WHAT NOW? · CAN RADR PREPARE/EXECUTE? · WHAT HAPPENED AFTER? · WAS VALUE VERIFIED? · THEN LEARN.

## Category

**Primary:** The adaptive decision system for hospitality.
**Supporting:** Verified Decision Intelligence · System of Decision Record.
**Named gap:** The Decision Gap — between what systems know and what the operation decides.

Public freeze: do not invent another category, rename Intelligence lenses, or expose positioning workshop language.

RADR ≠ PMS / POS / RMS / BI / scheduling / invoice automation / generic copilot.
RADR = persistent cross-system Decision layer. Do not claim competitors cannot expand adjacent.

## Loop

CONNECT → UNDERSTAND → FUTURES → DECIDE → VERIFY → REMEMBER

How a Decision is based: EVIDENCE → STATE → RELATE → BASELINE → FUTURES → VERIFY  
(STATE is internal — public loop uses UNDERSTAND.)

(Internal: OBSERVE → NORMALIZE → DERIVE → RELATE → DETECT → … → LEARN)

## Attention

NEEDS YOU · RADR IS HANDLING · WATCHING · HANDLED · NO ACTION

Prefer: HANDLE → PREPARE → ASK → INFORM

## Money vocabulary (marketing)

Only: **exposed / protected / recovered / created**. Always show horizon (tonight / this week / annual). Never naked euros. Never “upside / opportunity / actionable insights / AI-powered.”

## Trust language

Prefer “historically followed by / expected / associated with.” Never claim causation or Verified Value without an evidence chain. Never fake PMS/POS writes.

## Homepage

The homepage is the loop, not the company wiki.

Spine only: Nav → Hero Decision → Since your last check → Simulate one fork → One hidden signal as a Decision → Close.

Delete anything not in that spine.

## Platform

Systems record. RADR decides. Humans provide the hospitality.

One primary surface: **Brief** (Control Center) — what needs you / handling / verified since last check.

Not Platform surfaces: Ask RADR, Forecast-as-app, equal feature tiles for Findings / Actions / Value.

Findings = Why. Prepared Actions = payload. Verified Value = Decision after the night (Ledger). Connections + Operating model feed the twin.

## Shared Decision ID

Homepage hero peak === `/app` needs-you peak === verified proof: `dec_peak_berlin`.
Intelligence landing cross-domain flagship: `dec_menu_peak_berlin` (D-7110) — BUY × LABOR × SELL.
D-1842 (`dec_tuna_berlin`) remains in the demo library as a supplier / stock Decision — not the flagship restaurant story.

## Intelligence

BUY / LABOR / SELL / RECOVER are **economic lenses** over one normalized operating model — not four products.

**Futures** is the forward-looking beat inside the Decision — not a fifth lens. Intelligence plays forks via `IntelFuturesFork` (`futuresFromCanon`); Platform deep surface is `/product/futures`.

Source of truth: `lib/radr/intelligence/architecture.ts`
Demo registry: `data/demo/intelligence/`

Law: The number is not the intelligence. The connection is.
