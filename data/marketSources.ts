/**
 * Alias for canonical market registry.
 * Prefer `@/data/market/sources` in new code.
 */
export {
  MARKET_SOURCES,
  WHY_NOW_STATS,
  marketSource,
  type MarketSource,
  type MarketSourceId,
} from "@/data/market/sources";
