# Identity and tenancy

Every ingest and API call is bound to an organization (tenant).
Locations belong to organizations. Connections belong to tenants.
API keys and OAuth connections never cross tenants.
