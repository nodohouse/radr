"use client";

/**
 * Google / Microsoft OAuth is not wired. Hide the buttons until they work.
 * When live: label as ENTERPRISE SSO COMING SOON for SAML/OIDC separately.
 */
export function AuthSsoBlock() {
  return null;
}
