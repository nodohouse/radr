"use client";

/**
 * Location context lives in the top-bar operating lockup (`TopbarWhere`).
 * Kept as a no-op mount so ProductShell imports stay stable.
 */
export function LocationChrome() {
  return null;
}
